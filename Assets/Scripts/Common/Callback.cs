using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Larvend.Gameplay
{
    /*
    public class Callback
    {
        public delegate void CallbackDelegate(object obj);

        public Callback()
        {
            delegate Callback();
        }

        public void Invoke(CallbackDelegate callback)
        {
            callback(null);
        }
    }
    */
}